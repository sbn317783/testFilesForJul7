#ifndef juego_h
#define juego_h

void envidenciaChoose(); // aqui ira la seleccion de la evidencia 
void preEvidenceShownGame(); // we declare the function that will call all the other functions previous to the choosen of the gam 
void askVictorsLife (); // for this function we will stored the loop on how Victor ended up in Jail
void pedirNombreEjemplo(); // this function will ask the user for their name and store it in the struct Jugador
void excuseBeingLate(); // this function will ask the user for their excuse for being late
void pickUpEvidence(); // this function will ask the user to pick up evidence and show the information of Victor








#endif 
